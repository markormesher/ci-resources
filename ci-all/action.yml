name: "CI All"
inputs: {}
runs:
  using: "composite"
  steps:
    - name: "Check Jobs"
      shell: "sh"
      run: |
        results=$(grep '"result":' <<<'${{ toJson(needs) }}')
        if grep -v success <<<"$results"; then
          echo "One or more jobs was not successful" >&2
          exit 1
        fi
        echo "All jobs passed"
