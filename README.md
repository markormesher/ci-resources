# CI Resources

A small collection of actions/tools used in my public and private CI workflows. Intended to use only basic tools, allowing them work across any base image and not require a Node runtime.
